export enum HttpMethod {
  GET = `GET`,
  HEAD = `HEAD`,
  POST = `POST`,
  PUT = `PUT`,
  PATCH = `PATCH`,
  DELETE = `DELETE`,
  OPTIONS = `OPTIONS`,
  TRACE = `TRACE`
}

export enum SliceName {
  Auth = `auth`,
  Carousel = `carousel`,
  Order = `order`,
  Post = `post`
}
